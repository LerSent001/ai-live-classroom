# Video: this is the ONLY fal endpoint the app submits to:
# minimax/h3-max-turbo/image-to-video
FAL_KEY=

# Lesson planning goes directly to Gemini, with no provider fallback.
GEMINI_API_KEY=

# Save clips and scene JSON under recordings/<sessionId>/ (0 disables).
SAVE_RECORDINGS=1
