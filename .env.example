# Stable origin used for wallet callback and request origin validation.
TOKENPAY_PUBLIC_URL=http://localhost:3000
# Wallets connect through the browser; there is no shared API key fallback.
SAVE_RECORDINGS=1
